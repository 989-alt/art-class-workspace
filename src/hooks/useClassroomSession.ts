// TODO(v3-T2): deprecated — v2 classroom session hook tied to the old
// vote/submission schema. Removed in v3-T0; v3-T2 introduces useClassroom
// against the new LMS schema. Currently unused — kept as a stub so existing
// references compile while subsequent tasks rewrite the integration.
export {};
