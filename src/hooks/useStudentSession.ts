// TODO(v3-T4): deprecated — v2 student session hook for the vote model.
// Removed in v3-T0; v3-T4 introduces a new student-side hook against the LMS
// schema. Currently unused — kept as a stub so previously-referenced imports
// resolve while later tasks rewrite the integration.
export {};
