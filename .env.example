# API key for Gemini Nano Banana (BYOK — user-provided in-app at runtime)
# Not required here; the app prompts for it on first visit.

# Supabase (optional — only required for the "학급 모드" features)
# Leave blank to run in local-only mode.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
